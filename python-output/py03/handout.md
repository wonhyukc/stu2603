# 3주차: 변수와 마법의 문자열 조작 (Student Handout)

## 1. 개념 참고 영역 (Reference)

### (1) 변수 (Variables)란 무엇인가요?
변수(Variable)는 데이터를 담아두는 '상자' 또는 데이터에 이름표를 붙이는 것과 같습니다. 수학 시간에 배운 x, y와 비슷하지만, 파이썬에서는 이름표에 훨씬 더 자유로운 이름을 붙일 수 있습니다!
- **선언과 할당**: `age = 20` (age라는 이름의 상자에 20을 넣습니다)
- **복합 조작**: `age += 1`은 기존 나이에 1을 더한다는 뜻으로, `age = age + 1`과 완전히 동일합니다. 많이 쓰이니 눈에 익혀두세요!
- **이름 짓는 규칙 (snake_case)**: 단어와 단어 사이를 공백 대신 밑줄(`_`)로 연결하여 뱀처럼 보이게 합니다. (예: `student_name`, `my_age`)

### (2) 문자열 위치 찾기 (Indexing & Slicing)
'Hello' 라는 글자가 있을 때, 파이썬은 이 글자들의 순서를 **0부터 셉니다**. (이것은 프로그래머들의 세계적인 규칙입니다!)
`text = "Hello"`
- **인덱싱(콕 집어 꺼내기)**: `text[0]`은 첫 번째 글자인 `'H'`를 가져옵니다. `text[-1]`은 뒤에서 첫 번째, 즉 맨 끝 글자인 `'o'`를 가져옵니다. 외국인 유학생 여러분도 쉽게 이해할 수 있도록 맨 뒤부터는 -1, -2, -3으로 간다고 기억해 주세요.
- **슬라이싱(범위로 자르기)**: `[시작:끝]` 형태로 씁니다. 주의할 점은 끝맺는 번호는 **포함하지 않는다**는 것입니다! `text[0:4]`라고 쓰면 0, 1, 2, 3번째 글자인 `'Hell'` 까지만 가져옵니다.

### (3) 글자 요리하기 (String Methods)
문자열 내용물 뒤에 점(`.`)을 찍으면 문자를 예쁘게 다듬어주는 여러 가지 **도구(메서드)**를 불러올 수 있습니다.
- `.upper()` / `.lower()`: 글자를 모두 대문자(ABC)/소문자(abc)로 바꿉니다.
- `.replace("A", "B")`: "A"라는 글자를 찾아서 모두 "B"로 바꿉니다. (예: `text.replace("e", "a")` -> `"Hallo"`)
- `.split()`: 주로 큰따옴표 안의 문장을 공백이나 특정 기준을 통해 쪼개줍니다. 나누어진 단어들은 리스트라는 곳에 쏙 들어갑니다.
- `len(text)`: 텍스트의 전체 길이(글자 수)를 알려줍니다. (length의 줄임말입니다.)

### (4) 궁극의 요술지팡이, f-string (문자열 포매팅)
문장을 출력할 때, 내 변수를 넣고 싶을 때가 많죠? 그럴 땐 항상 앞쪽에 `f`를 붙이고 내용물 안에 중괄호 `{}`를 쓰세요. 그곳에 변수를 넣을 수 있습니다.
`name = "John"`
`print(f"My name is {name} and it is nice to meet you!")` -> 화면에 진짜 내 이름이 들어간 예쁜 문장이 출력됩니다!

---

## 2. Lab Assignment (실습 과제 - 상호평가 대상)

오늘 영상 강의(lecture)를 잘 보셨나요? 이제 배운 내용을 바탕으로 Google Colab에서 파이썬 코드를 직접 작성해 봅시다.
완성한 코드는 캡처하여 **eCampus 실습 과제 제출 항목**에 기한 내에 제출하세요. 동료 학생들끼리 서로의 결과를 교차 확인하는 상호평가(Peer Review)가 진행됩니다!

**실습 진행 팁!**
다운로드 받은 `lab.ipynb`을 Google Colab에서 열고 셀(Cell)을 하나씩 실행해 보세요. 에러가 나면 부끄러워하지 말고, AI 파트너(ChatGPT, Gemini)에게 에러 코드를 붙여넣어 조언을 구해보는 것을 절대 잊지 마세요. 스텝·코드 나열은 최소화했으니, 여러분 스스로 논리 흐름을 만들어 보세요!

---

## 3. Micro-Assignment (마이크로 과제 - 이메일 제출 대상)

이번 학기 가장 중요한 시간입니다! 매주 진행되는 마이크로 과제입니다.

**제출처**: 정원혁 교수님 메일 (`wonhyukc@stu.ac.kr`)
**메일 제목 규칙 (엄수!)**: `과제0.3 학번` (예시: `과제0.3 20240001` - 만약 이 규칙을 어기면 이메일 자동 필터링 시스템을 통과하지 못해 0점 처리됩니다!)
**분량**: A4 용지 0.5페이지 내외. 짧고 굵게 적어주세요.

이번 주 과제는 다음 세 가지 내용을 포함해서 이메일 본문에 바로 적어주세요.

1. **[수업 소감]**: 오늘 3주차 실습에서 가장 흥미로웠던 점은 무엇이었나요? 혹은 제일 어려웠던 것은요?
2. **[주차별 추가 실습]**: 본인의 Colab 노트북에서 f-string을 활용했던 실습 코드를 변형해보세요. 이름 외에 좋아하는 음식, 나의 고향, 나이 등 2가지 이상의 파라미터를 추가로 `input()` 함수로 입력받게 하고, 새로운 포맷의 결과 화면을 만들어서 캡처 이미지를 본문에 첨부해주세요! 추가적으로, 당신의 코드가 어떻게 기존 구조에서 바뀌었는지 요리나 일상생활의 재미있는 **비유**를 들어 설명해 보세요.
3. **[AI 사용 경험 - 초과 달성 코너]**: AI 사용은 필수입니다!
   - **(Prompt)**: 방금 변형한 당신의 코드를 그대로 AI에게 붙여넣고 이렇게 물어보세요. "안녕 AI야, 이 코드를 조금 더 우아하고 짧게 만들 수 있을까? 혹은 문자열 메서드를 하나 더 쓴다면 어떻게 바꿀지 추천해줘!"
   - **(Critique)**: AI가 다시 짜준 코드를 유심히 읽어보세요. 여러분이 짠 코드와 비교해서 무엇이 어떻게 달라졌나요? 그 코드를 그대로 썼을 때의 장단점은 무엇일까요?
   - **(Result)**: 이 비평 과정을 통해 새롭게 알게 된 점이나 코드 수정 사항을 종합하여 짧게 요약해 주세요.
