# py02 Lecture & Slide QA 루브릭

**평가 대상 파일**
- `python-output/py02/lecture.md`
- `python-output/py02/lecture-script.md`
- `python-output/py02/slides.md`

**평가 기준 SSOT**
- `python-output/컴퓨팅적사고와코딩(K트랙)_교육안.md` — 2주차 항목
- `CLAUDE.md` — 품질 기준 (분량·언어·환경)

**만점**: 100점
**PASS 기준**: 85점 이상 AND 파트 A ≥ 55점 AND 파트 B ≥ 28점
**FAIL 시**: 보완 항목 목록 출력 후 Auto-Fix

---

## 파트 A. Lecture (lecture.md + lecture-script.md) — 65점

---

### A-1. lecture.md 구조 및 정확성 (15점)

#### A-1-1. 기본 정보 표 정확성 (3점)
- [ ] **3점**: 주차·주제·실습환경·사전영상·총 시간이 모두 교육안 및 CLAUDE.md 기준과 일치한다.
  - 주제: **출력·입력과 기본 타입** (현재 "개발 환경 및 출력"은 오기)
  - 실습 환경: **Google Colab** (StackBlitz 표기 시 FAIL)
  - 총 시간: **75분 (25분 × 3섹션)**
- [ ] **1점**: 1~2개 항목이 부정확하다.
- [ ] **0점**: 3개 이상 항목이 오기이거나 누락이다.

#### A-1-2. 학습 목표 교육안 일치 (7점)
교육안 2주차 학습 목표 3개가 모두 반영되어 있는지 항목별 채점.

| 목표 항목 | 배점 | 평가 기준 |
|:----------|-----:|:----------|
| `print()`로 문자열·숫자·여러 값을 다양한 형식으로 출력한다 | 2 | 키워드(`print()`, 여러 값, 형식) 포함 여부 |
| `str`·`int`·`float`·`bool` 4가지 기본 타입과 산술 연산자를 구별한다 | 3 | 타입 4종 및 연산자 언급 포함 여부 |
| `input()`으로 사용자 입력을 받고 타입 변환을 적용한다 | 2 | `input()` 및 타입 변환 언급 포함 여부 |

> **주의**: 현재 lecture.md의 학습 목표는 교육안 기준과 불일치 — Colab 사용법·에러 디버깅 중심으로 기술되어 있으며 `input()`, 4가지 타입, 연산자 목표가 없음.

#### A-1-3. 섹션별 시간 배분 표 정확성 (5점)
- [ ] **5점**: S1·S2·S3 각 25분 배정이 표로 명시되어 있고, 각 섹션 주제·핵심포인트가 교육안과 일치한다.
  - S1: print() 완전 정복 (end=, sep=, 주석 포함)
  - S2: 4가지 타입 + type() + 산술 연산자 7종
  - S3: input() + 타입 변환 + 에러 분석
- [ ] **3점**: 섹션 구분은 있으나 1~2개 섹션의 주제가 교육안과 다르다.
- [ ] **0점**: 섹션 구분 자체가 교육안과 무관하다.

---

### A-2. lecture-script.md 내용 충실도 (50점)

#### A-2-1. 분량 및 품질 규정 (10점)

| 기준 | 배점 |
|:-----|-----:|
| 전체 단어 수 4,500단어 이상 (한국어 기준 약 9,000자 이상) | 6 |
| 동일 문장·문단의 기계적 반복이 3회 이상 없음 (어뷰징 검사) | 2 |
| 구어체(강사 말투)로 작성되어 있고 볼드체·문단 구분 충분 | 2 |

> **현재 상태**: 약 800단어 수준으로 기준 대비 심각 미달. 반드시 전면 보강 필요.

#### A-2-2. S1 내용 완전성 — print() 완전 정복 (15점)

| 항목 | 배점 | 확인 기준 |
|:-----|-----:|:----------|
| `print()` 기본 사용법 (문자열·숫자 출력) | 3 | `print("텍스트")`, `print(숫자)` 시연 포함 |
| 여러 값 동시 출력: 콤마(`,`) vs `+` 연산 차이 | 4 | 두 방식의 동작 차이를 명시적으로 비교 설명 |
| `end=` · `sep=` 옵션 설명 | 5 | 각 키워드 의미·예시 코드 포함 |
| 주석(`#`) 의미와 사용법 | 3 | `#` 이후 텍스트가 무시되는 원리 설명 |

> **현재 상태**: 기본 print() 시연은 있으나 콤마/+ 차이 언급 없음, end=/sep= 완전 누락, 주석 누락.

#### A-2-3. S2 내용 완전성 — 4가지 타입 + 연산자 (15점)

| 항목 | 배점 | 확인 기준 |
|:-----|-----:|:----------|
| `str` · `int` · `float` · `bool` 4가지 타입 설명 | 6 | 타입별 정의·예시 각각 제시 (bool 포함 필수) |
| `type()` 함수 사용법 | 3 | `type(값)` 으로 타입 확인하는 시연 |
| 산술 연산자 7종 (`+` `-` `*` `/` `//` `%` `**`) | 6 | 7종 모두 명시, 특히 `//`(정수 나눗셈)·`%`(나머지)·`**`(거듭제곱) 포함 |

> **현재 상태**: float, bool, type() 미등장. 연산자는 +와 * 일부만 언급. 4가지 타입 섹션 없음.

#### A-2-4. S3 내용 완전성 — input() + 타입 변환 + 에러 (10점)

| 항목 | 배점 | 확인 기준 |
|:-----|-----:|:----------|
| `input()` 함수 사용법 (사용자 입력 받기) | 4 | `input("프롬프트")` 예시, 반환값이 str임을 설명 |
| 타입 변환 함수 `int()` · `str()` · `float()` | 4 | 각 함수 의미·사용 예시 포함 |
| `SyntaxError` · `TypeError` 원인 분석 | 2 | 두 에러 유형의 원인을 구별하여 설명 |

> **현재 상태**: `input()` 완전 누락. 타입 변환 함수 누락. 에러 설명은 SyntaxError만 있고 TypeError 누락.

---

## 파트 B. Slides (slides.md) — 35점

---

### B-1. 슬라이드 기본 구조 (5점)
- [ ] **5점**: 타이틀 슬라이드(강사명 포함)·목차·섹션별 본문·마무리 슬라이드가 모두 존재한다.
  - 타이틀에 **정원혁** 강사명 표기 필수
- [ ] **2점**: 구조는 있으나 타이틀 강사명 누락 또는 마무리 슬라이드 없음.
- [ ] **0점**: 최소 구조 미충족.

### B-2. S1 커버리지 — print() 완전 정복 (10점)

| 항목 | 배점 |
|:-----|-----:|
| 콤마(`,`) vs `+` 차이를 보여주는 코드 비교 슬라이드 | 4 |
| `end=` · `sep=` 옵션 슬라이드 (예시 코드 포함) | 4 |
| 주석(`#`) 설명 슬라이드 | 2 |

> **현재 상태**: 기본 print() 슬라이드는 있으나 콤마/+ 비교, end=/sep=, 주석 슬라이드 모두 누락.

### B-3. S2 커버리지 — 4가지 타입 + 연산자 (10점)

| 항목 | 배점 |
|:-----|-----:|
| 4가지 타입(`str`·`int`·`float`·`bool`) 비교표 또는 슬라이드 | 4 |
| `type()` 함수 시연 슬라이드 | 3 |
| 산술 연산자 7종 정리 슬라이드 (`//`·`%`·`**` 포함) | 3 |

> **현재 상태**: 문자 vs 숫자 비교는 있으나 bool·float 미포함, type() 없음, 연산자 7종 정리 없음.

### B-4. S3 커버리지 — input() + 타입 변환 (10점)

| 항목 | 배점 |
|:-----|-----:|
| `input()` 사용법 슬라이드 (예시 코드 + 반환값이 str임을 명시) | 5 |
| 타입 변환 함수(`int()`·`str()`·`float()`) 슬라이드 | 5 |

> **현재 상태**: input() 슬라이드 완전 누락. 타입 변환 슬라이드 없음.

---

## 종합 평가 결과표

| 파트 | 항목 | 만점 | 취득 | 감점 이유 |
|:-----|:-----|-----:|-----:|:----------|
| A | A-1-1. 기본 정보 정확성 | 3 | | |
| A | A-1-2. 학습 목표 일치 | 7 | | |
| A | A-1-3. 섹션 배분 정확성 | 5 | | |
| A | A-2-1. 분량 및 품질 | 10 | | |
| A | A-2-2. S1 print() 완전 정복 | 15 | | |
| A | A-2-3. S2 타입+연산자 | 15 | | |
| A | A-2-4. S3 input()+타입변환+에러 | 10 | | |
| B | B-1. 슬라이드 기본 구조 | 5 | | |
| B | B-2. S1 커버리지 | 10 | | |
| B | B-3. S2 커버리지 | 10 | | |
| B | B-4. S3 커버리지 | 10 | | |
| | **합계** | **100** | | |
| | **파트 A 소계** | **65** | | |
| | **파트 B 소계** | **35** | | |

### PASS / FAIL 판정 기준

| 조건 | 기준 |
|:-----|:-----|
| 총점 | **85점 이상** |
| 파트 A (Lecture) | **55점 이상** |
| 파트 B (Slides) | **28점 이상** |

세 조건을 **모두** 충족해야 PASS. 하나라도 미달 시 FAIL.

### FAIL 시 보완 우선순위

1. **[최우선] A-2-4**: `input()` 함수 및 타입 변환 S3에 추가
2. **[최우선] A-2-1**: lecture-script.md 전체 분량 보강 (4,500단어 이상)
3. **[고우선] A-2-2**: end=, sep=, 주석(#) S1에 추가
4. **[고우선] A-2-3**: float, bool, type(), 연산자 7종 S2에 추가
5. **[고우선] A-1-2**: lecture.md 학습 목표를 교육안 3개 목표로 교체
6. **[중간] B-4**: input()+타입변환 슬라이드 신규 추가
7. **[중간] B-2**: end=/sep=/주석 슬라이드 추가
8. **[중간] B-3**: type()/연산자 7종/bool 슬라이드 추가

### 주요 발견 사항 (검토자 작성)

**잘된 점**:

**보완 필요 사항**:

**검토일**: &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; / **검토자**:
