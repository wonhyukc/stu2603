# 2주차 핸드아웃 — 출력·자료형·연산자·입력 기초

**컴퓨팅적사고와코딩 | K트랙 | 2주차**
강사: 정원혁 (wonhyukc@stu.ac.kr)

---

## 1. 개념 참고 영역

강의 영상을 시청한 후 복습 자료로 활용하세요.

---

### 1-1. print() 옵션 요약

| 요소 | 설명 | 예시 | 출력 결과 |
|:-----|:-----|:-----|:---------|
| `print("값")` | 기본 출력 | `print("안녕")` | `안녕` |
| 콤마(`,`) | 공백 자동 삽입, 타입 혼합 가능 | `print("나이:", 20)` | `나이: 20` |
| `+` 연결 | 공백 없이 붙임, 문자열끼리만 | `print("안녕" + "하세요")` | `안녕하세요` |
| `end=` | 출력 끝 문자 변경 (기본: `\n`) | `print("A", end="")` | 줄바꿈 없이 `A` |
| `sep=` | 값 사이 구분자 변경 (기본: 공백) | `print("a","b", sep="-")` | `a-b` |
| `#` 주석 | 실행되지 않는 설명 메모 | `# 이 줄은 무시됨` | (출력 없음) |

---

### 1-2. 4가지 기본 자료형 참고표

| 타입 | 이름 | 예시 | 특징 |
|:----:|:-----|:-----|:-----|
| `str` | 문자열 | `"안녕"`, `"010-1234"` | 따옴표로 감싼 텍스트 |
| `int` | 정수 | `42`, `-5`, `0` | 소수점 없는 숫자 |
| `float` | 부동소수점 | `3.14`, `175.5` | 소수점 있는 숫자 |
| `bool` | 불린 | `True`, `False` | 참/거짓, 대문자 시작 필수 |

- `type(값)` 으로 타입을 즉시 확인할 수 있습니다.
- `"42"` (따옴표 있음) vs `42` (따옴표 없음): 완전히 다른 타입입니다.

---

### 1-3. 산술 연산자 7종

| 연산자 | 이름 | 예시 | 결과 | 비고 |
|:------:|:-----|:-----|:----:|:-----|
| `+` | 덧셈 | `10 + 3` | `13` | |
| `-` | 뺄셈 | `10 - 3` | `7` | |
| `*` | 곱셈 | `10 * 3` | `30` | |
| `/` | 나눗셈 | `10 / 3` | `3.333...` | 결과는 항상 `float` |
| `//` | 정수 나눗셈 (몫) | `10 // 3` | `3` | 소수점 버림 |
| `%` | 나머지 | `10 % 3` | `1` | 짝수/홀수 판별에 활용 |
| `**` | 거듭제곱 | `2 ** 3` | `8` | 2의 3제곱 = 8 |

---

### 1-4. input() + 타입 변환 요약

**핵심 규칙**: `input()` 의 반환값은 **항상 `str`** 입니다.

| 상황 | 코드 패턴 | 설명 |
|:-----|:---------|:-----|
| 숫자 계산이 필요할 때 | `int(input("입력: "))` | 정수로 변환 후 사용 |
| 소수점 계산이 필요할 때 | `float(input("입력: "))` | 부동소수점으로 변환 |
| 숫자를 문자열에 붙일 때 | `str(숫자)` | 문자열로 변환 후 `+` 사용 |

**에러 구별**

| 에러 | 발생 시점 | 주요 원인 | 해결 방법 |
|:-----|:--------:|:---------|:---------|
| `SyntaxError` | 실행 전 | 괄호 누락, 따옴표 미완성 등 | 해당 줄 문법 재확인 |
| `TypeError` | 실행 중 | `str + int` 등 타입 충돌 | 타입 변환 함수 사용 |

---

## 2. 실습 섹션 (Lab Assignment)

이번 주 실습은 `lab.ipynb` 파일을 Google Colab에서 열어 진행합니다.
eCampus 2주차 강의 자료에서 `lab.ipynb` 파일을 다운로드한 후, Google Colab에 업로드하여 실습하세요.

### 상호평가 실습 과제: "나만의 미니 키오스크 영수증 만들기"

강의와 실습(`lab.ipynb`)을 모두 마친 후, 배운 내용(`print()`, `input()`, 변수, 연산자)을 활용하여 아주 간단한 프로그램을 스스로 작성합니다. Colab의 맨 하단 셀에 코드를 작성하세요.

**[과제 요구사항]**
1. 사용자에게 `커피 이름`, `가격`, `수량`을 `input()`으로 각각 입력받습니다.
   (이때 가격과 수량은 `int()`를 사용해 올바르게 숫자로 변환해야 함)
2. 총액(가격 * 수량)을 계산하는 연산자를 사용합니다.
3. `print()` 옵션을 자유롭게 사용하여 최종 영수증 결제 금액이 멋지게 화면에 출력되게 하세요.

**[상호평가 제출 절차 (매주 반복)]**
1. 작성한 실습 노트북(`lab.ipynb` 또는 새로 만든 스크립트)을 본인의 **GitHub 리포지토리(Public)** 로 복사/저장(Commit)합니다. (Colab의 '파일 > GitHub에 사본 저장' 기능 활용)
2. 저장 후 표시되는 여러분의 **GitHub 커밋(Commit) 링크**를 복사합니다.
3. eCampus **[2주차 상호평가 과제 게시판]**에 해당 링크를 제출합니다.
4. 제출 후, 무들(Moodle) 시스템의 안내에 따라 무작위로 배정받은 동료 학생 **3명**의 링크를 열어서 상호평가 루브릭(O/X 3점 기준)에 따라 채점해 줍니다. 

---

## 3. 과제 섹션 (Micro-Assignment)

이번 주 마이크로 과제는 아래 항목들을 작성하여 **이메일**로 제출해야 합니다.

### 1. 수업 소감
- 이번 주 수업을 듣고 느낀 점, 어려웠던 점, 또는 건의 사항을 자유롭게 작성해 주세요.

### 2. 이번 주 미션: 의도적인 에러 발생 및 검토
이번 주 강의에서 배운 `print()`, 타입, 연산자, `input()` 중 하나와 관련된 **에러를 의도적으로 발생**시켜보세요. (예: `input()`으로 받은 값에 바로 숫자를 더하기, 문자열에 숫자를 `+`로 연결하기 등) 에러 화면을 캡처하여 결과물에 포함합니다.

### 3. AI 사용 경험 (Prompt / Critique / Result)
위에서 발생시킨 에러 메시지를 AI(ChatGPT 또는 Gemini)에게 보여주고 원인을 물어보세요. 그 과정을 아래 3단 형식으로 정리합니다.

- **Prompt**: AI에게 던진 질문을 그대로 적으세요.
  > 예: "파이썬에서 `TypeError: can only concatenate str (not "int") to str` 에러가 났는데 왜 이런 에러가 나는 거야?"
- **Critique**: AI의 답변 중 맞는 부분과 틀리거나 애매한 부분을 **한 문장**으로 요약하세요.
  > 예: "AI가 str + int 충돌 원인은 정확히 설명했지만, 해결책으로 f-string을 권장한 것은 이번 주차에서 배우지 않은 방법이라 적용하기 어려웠다."
- **Final Result**: AI의 답변을 바탕으로 본인이 직접 수정한 최종 코드 또는 에러 원인에 대한 본인의 설명을 적으세요.

---

### 마이크로 과제 제출 안내

- **제출처**: `wonhyuk@stu.ac.kr` (이메일 제출)
- **제출 기한**: 매주 수업 전날 자정까지 (정확한 일정 eCampus 참조)
- **메일 제목 형식**: `과제0.2 학번` (예: `과제0.2 20250001`)
- **⚠️ 주의사항**: 메일 제목을 코드로 자동 필터링하기 때문에, 규칙을 지키지 않으면 채점에서 누락됩니다. 제목에는 반드시 "과제0.2", 빈칸 하나, 그리고 "학번"만 적어야 하며 다른 어떠한 문자도 포함하지 마세요.
- **분량**: A4 0.5페이지 내외 (에러 화면 캡처 이미지 포함 권장)
